#
# :Precis: Config the host for IBM MQ installs
# :Authors: Andrew Burrow
# :Contact: andrew.burrow@syntegrity.com.au
#

---

galaxy_info:
  author: Andrew Burrow
  description: >-
    Config the host for IBM MQ installs

    This *action role* sets up the host for environment installs of IBM MQ.

    See `../../ibm_mq_host_define/defaults/main.yml` for details on the
    parameters.
  company: Syntegrity Solutions

dependencies:
  - role: ibm_mq_host_define
